# Project: Bachelor Trip Site (GitHub Pages)

## What this is
A static site for a 10-man bachelor trip, Sept 21–27, 2026. Two standalone pages, no shared code:

- **`index.html`** — the groomsmen-facing homepage: countdown, updates feed, and (once the destination is chosen) the itinerary. Currently shows the "destination being decided" state.
- **`destination-choices/index.html`** — the destination-options page for the groom + best man ONLY. Passphrase-gated (`zzdeathscopezz`, client-side, cosmetic). Carousel UI: one option open in a big viewer (photos, quick-fact chips, activity tags, scorecard, pros/cons, day/night itinerary), the current board in a strip pinned to the bottom, and everything cut or superseded behind an Archive toggle. Reached only by knowing the URL: `https://arathie.github.io/jaipal-bachelor-trip/destination-choices/`.

## Hard constraints (do not violate)
1. **Secrecy**: groomsmen must not see the candidate options or scores. All candidate data (`TRIPS`, statuses, costs, scores) lives ONLY in the options page. `index.html` must never contain candidate data, the passphrase, the `destination-choices` path, or any link/reference to it — no UI elements, no comments, no source strings.
2. **The options page is permanent — never delete it** (owner decision, July 2026). After the destination is chosen, the unchosen options stay there at the same URL, unlinked from the homepage. The owner picked the slug himself, knows the repo is public, and accepts that someone could dig it up.
3. **No surprises leaked**: never add content about gifts, finale plans, or who pays what. The groom reads both pages. The budget cap is also private — never show it on either page.
4. **Tone is plain and unpretentious** (owner request, July 2026): no military/ops/spy metaphors ("HQ", "dossier", "classified", "ops", "riders", "the call"), no self-important framing of the groom/best man decision process. Plain enthusiasm about real activities is fine; theatrical copy is not.
5. **No build step**: two self-contained HTML files plus static images under `destination-choices/img/`. Vanilla JS only. No frameworks, no bundlers, no external JS. Google Fonts is the only external resource — photos are committed to the repo, never hotlinked.
6. **Design system is fixed** (topo field-map): tokens at the top of each `<style>` block — bone/contour/ink/river/blaze/pine, Big Shoulders Display + Public Sans + IBM Plex Mono. Blaze orange is reserved for anchor-status and active markers only. Don't restyle; extend within the system. Keep the two pages visually consistent.
7. Keep `noindex` meta on both pages. Keep the site mobile-first (~380px is the check width).
8. **Images must be freely licensed** (public repo): Wikimedia Commons / Unsplash / Pexels / public domain only, with attribution tracked in `destination-choices/img/CREDITS.md` and shown in the photo caption line.

## Deploy status (as of July 10, 2026)
- All work is committed and pushed to branch `claude/bachelor-trip-website-5nm5m3`, which is the repo's **default branch** (the repo was created empty and this was the first push).
- **GitHub Pages is not yet enabled** — the owner's one manual click: Settings → Pages → Source "Deploy from a branch" → branch `claude/bachelor-trip-website-5nm5m3`, folder `/ (root)`. Then the homepage is `https://arathie.github.io/jaipal-bachelor-trip/` and the options page is `https://arathie.github.io/jaipal-bachelor-trip/destination-choices/` (deep links: `#aruba`, `#moab`, …).
- Next expected site change: the groom picks a destination → follow the decision-day runbook below. The offline next step per the shortlist is dated September quotes on the finalists.

## Current state
- Homepage (`index.html`): plain hero, live countdown to `TRIP_START`, groomsmen-safe `UPDATES` feed. `CONFIG.CHOSEN_TRIP` is null.
- Options page (`destination-choices/index.html`): reflects the July 2026 pivot to the villa formula (staffed villa + private chef + beach drinks + one private boat day; optional modules: jet skis, fishing, ATVs, snorkeling). Passphrase gate → hero with status tally + **Archive toggle** + compare-table toggle → viewer (selected option: photos with captions/credits, score gauges, why-it's-listed, day/night itinerary, requirements, flags, timing) → sticky bottom strip (tap to open, ◀ ▶ and arrow keys, URL hash deep-links; a hash pointing at an archived option auto-enables the archive).
- Data: 20 entries in `TRIPS`, in this order (ids double as URL hashes): board — `aruba, cartagena, cabo, grenada, barbados, lakepowell` (6 CONTENDER, shortlist order) + `curacao, roatan` (2 MONITOR); archive — `casadecampo, bocas, costarica` (villa-era `status:"CUT", archived:true`, cut reason as the first flag) + `argentina, gauley, whistler, moab, sanjuans, sandiego, texas, skydive, rally` (9 ARCHIVED adventure-era). Everything `archived:true` is hidden from strip/compare unless the Archive toggle is on. **Archived/cut entries are permanent** (owner decision) — same rule as constraint 2. Scores are keyed per rubric: contenders `scores:{ST,NL,AC,NV,VQ}` (storm-risk scored as safety / nightlife / activities / novelty / villa quality, higher = better, from the shortlist scorecard); the adventure archive keeps `{A,F,S}` sliders as an era record. Monitors and villa-era cuts still carry `{C,F,W}` in data (historical), but **no C/F/W sliders ever render** — they show a "not on the scorecard" note instead (owner correction, July 2026). The compare table is the five-column scorecard + Total; non-scorecard rows show dashes. Board entries also carry `mods:[{k,v,n}]` (activity tag bubbles, v: 0 none · 1 partial/caveat · 2 available · 3 strong, n = short note), `temps:[hi,lo]` (late-Sept °F averages) and `travel:{nyc,atl,clt}` (estimated door-to-door strings, rendered as quick-fact chips); contenders add `pros[]`, `cons[]`, `villa`. Common fields: `id, name, region, status, cost:[lo,hi], flight, wow, second, gates[], flags[], fit, days:[{d, day, night}], photos:[{src, thumb, alt, caption, credit}]`.
- Data provenance: statuses, scorecard numbers (`ST/NL/AC/NV/VQ`), costs, pros/cons, villa notes, weather-lane and rain-test framing, and the NYC door-to-door times all come from the owner's shortlist doc ("narrowing round", July 10, 2026) — **don't invent or adjust scores**; monitors get scored only if a future shortlist scores them. `temps` are approximate late-September climate normals; the **ATL/CLT door-to-door times are route-based estimates (hence ≈) that still need checking against real schedules** when flights get priced. Venue/operator facts in the data (bike-park closing dates, rental-contract bans, club opening nights, etc.) were web-verified July 2026.
- Planning source docs (the brief/shortlist/archive) arrive as chat uploads each session. They contain groom-secret logistics (rigged finale, superlatives, who-pays). **Never commit them to this public repo and never copy those details onto the site** — constraint 3 applies when transcribing itineraries (the shortlist's own itinerary lines include "superlatives" and a finale "the groom mysteriously wins"; both were deliberately kept off the site).

## Decision-day runbook (when the destination is chosen)
1. Copy the winning trip's object out of the options page's `TRIPS` array and paste it as `CONFIG.CHOSEN_TRIP` in `index.html`. Only the winner — no other option ever enters the homepage. Strip the `photos` array (or copy the image files out of `destination-choices/img/` into a homepage-safe location first — homepage must not reference the `destination-choices` path). Optionally add `nameHTML` with explicit `<br>` breaks for the hero heading; it falls back to `name` wrapping naturally.
2. Refine its `days[]` into the real schedule with times and addresses as bookings land; add `UPDATES` entries on the homepage.
3. Leave the options page exactly where it is (constraint 2).

## Roadmap
1. Trip-mode additions when asked: flights table per person, packing list, payment-status board (names + paid y/n — get explicit OK before publishing anyone's payment status), FAQ, weather forecast link, map links per stop.
2. Nice-to-haves: print stylesheet for a one-page itinerary; countdown favicon.

## Conventions
- Homepage content edits happen in `CONFIG` / `UPDATES`; options-page content edits in its `TRIPS` array.
- Commit style: short imperative ("Add itinerary day cards"). **Commit identity is the owner's** — a SessionStart hook in `.claude/settings.json` sets repo-local `user.name`/`user.email` (Ash Rathie) in cloud sessions. Don't add AI co-author/attribution trailers; the owner rewrote history once to remove them (PR #1).
- After any change, verify on mobile width (~380px) before pushing.
- This remote environment has no `gh` CLI — use the GitHub MCP tools for GitHub operations. GitHub Pages serves from the repo (Settings → Pages, deploy from a branch).

## Working notes for future sessions
- **Photos**: files are `destination-choices/img/<id>-N.jpg` (resized to max 1200px wide, JPEG quality ~75) plus `<id>-thumb.jpg` (480px, generated from photo 1; the strip uses it via `photos[0].thumb`). Every image must be freely licensed (constraint 8); when adding one, append a line to `img/CREDITS.md` in its existing format (filename — caption. credit. License. Source. ). Wikimedia Commons direct-download trick: `https://commons.wikimedia.org/wiki/Special:FilePath/<File_Name>?width=1600`.
- **Verification workflow used so far**: serve the repo root with `python3 -m http.server`, then drive both pages headless at 380px width (in Claude remote envs: `npm i playwright-core`, launch with `executablePath: "/opt/pw-browsers/chromium"`). Minimum checks: gate rejects wrong passphrase and opens on `zzdeathscopezz`; strip shows 8 cards (20 with the Archive toggle on, divider before the archive); a hash for an archived id (e.g. `#moab`) auto-enables the archive; compare table = 5 scorecard columns + Total with dashes on unscored rows; C/F/W never renders as sliders; hero photos and strip thumbs load; no horizontal overflow at 380px.
- **Options-page JS invariants**: statuses are CONTENDER/MONITOR/CUT/ARCHIVED (colors river/pine/ink-soft/ink-soft; blaze marks only the active strip card); everything `archived:true` is excluded from strip/compare/stepping unless `showArchive`; the gate needs the `.gate[hidden]{display:none}` rule (the class's `display:flex` would otherwise override the `hidden` attribute — this was a real shipped bug once); `esc()` must keep escaping `"` because captions/flags contain quotes that land in HTML attributes.
- The homepage must stay free of every options-page string — after edits, grep it for the slug, the passphrase, and destination names (it should come back clean).
